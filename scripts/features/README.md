# scripts/features/

- `embeddings_dinov2.py` — embeddings via DinoV2 pré-treinado (Fase A).
- `noiseprint.py` — mapa de ruído via TruFor/NoisePrint++ (Fase B, opcional).

Saída gravada em `datasets/processed/`.
